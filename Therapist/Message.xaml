<UserControl x:Class="Therapist.Message"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Therapist"
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="320"  >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>
        <Button x:Name="ButtonClose" Background="{x:Null}" BorderBrush="{x:Null}" Click="buttonCancel_Click"  Grid.Column="1"  Foreground="Black" >
            <materialDesign:PackIcon Kind="Power"  Foreground="Black"/>
        </Button>
        <Rectangle Grid.RowSpan="2" Grid.ColumnSpan="2"/>
        <Label FontSize="22" Content="Сообщение" FontStyle="Italic" Margin="98,0,58,0" />
        <TextBlock Name="Content" Grid.Row="1" FontSize="18" Text="defrgty" VerticalAlignment="Center" HorizontalAlignment="Center"  Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>
